
$mobile = 320px
$desktop = 1000px
$desktop-md = 1400px
$desktop-lg = 1600px

$container-width = 960px
$container-width-lg = 1200px

$screens = {
    desktop-lg: $desktop-lg,
    desktop-md: $desktop-md,
    desktop: $desktop,
    mobile: $mobile
}

$base-font-size = 16px
$base-font-family = 'Avenir', Helvetica, Arial, sans-serif

$ease-out-quart = cubic-bezier(.165, .84, .44, 1)
$out-cubic = cubic-bezier(.22, .61, .36, 1)
$ease-out-cubic = cubic-bezier(.215, .610, .355, 1)
$in-out-cubic = cubic-bezier(.65, .05, .36, 1)
$ease-in-quart = cubic-bezier(.48, 0, .12, 1)


$black = #000
$white = #fff
$error = #d0021b
$success = #42b983

$base-row-height = 50px
